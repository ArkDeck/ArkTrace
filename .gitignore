# macOS
.DS_Store

# Swift Package Manager
/.build
.swiftpm/

# Xcode
DerivedData/
xcuserdata/
*.xcresult
# `xcodebuild -project ArkTrace.xcodeproj` writes ArkTrace.app here
/build

# Profiling / scratch
*.profraw

# Pinned TraceStreamer binary is built locally via scripts/build_trace_streamer.sh;
# only the provenance manifest is committed.
ThirdParty/TraceStreamer/macx/trace_streamer

# API baseline consumer build products
scripts/api-baseline/.build/
